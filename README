# Info

Node image for build purposes.

## Installs

- npm@6.0.1: this enables 'ci' command along with latests improvements
- python, g++, make: this permits to build the modules that require a build step after download
